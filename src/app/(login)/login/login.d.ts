export interface FormState {
    email: string;
    password: string;
}
export interface ValidState {
    buttonDisabled: boolean;
    labelButton: string;
    email: boolean;
    password: boolean;
}
